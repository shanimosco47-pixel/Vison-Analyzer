# Runtime dependencies. Bounded versions so an upstream major release cannot
# change behaviour of an installation made months from now.
#
#   pip install -r requirements.txt
#
# All four are mature, widely adopted and permissively licensed
# (Apache-2.0 / BSD-3-Clause); see README, "Dependencies and licensing".
opencv-python-headless>=4.9,<5   # Apache-2.0  - video decoding and all vision work
# BSD-3-Clause - array maths. <2.5: NumPy 2.5 adopted PEP 695 stub syntax
# mypy rejects under this project's python_version=3.10 config (see
# pyproject.toml's dependencies comment and python/mypy#21178).
numpy>=1.26,<2.5
flask>=3.0,<4                    # BSD-3-Clause - local web server
pandas>=2.1,<3                   # BSD-3-Clause - optional DataFrame export of the event log
